ktor {
    deployment {
        # 8081 avoids clashes with other tools that bind :8080 (e.g. local Python servers).
        port = 8081
        host = "0.0.0.0"
    }
    application {
        modules = [ com.xremail.backend.ApplicationKt.module ]
    }
}

xrmail {
    server {
        baseUrl = ${XRMAIL_BASE_URL}
    }
    jwt {
        secret = ${JWT_SECRET}
        issuer = ${XRMAIL_BASE_URL}
        audience = "xrmail-android-client"
        expirationMs = 604800000
    }
    google {
        clientId = ${GOOGLE_CLIENT_ID}
        clientSecret = ${GOOGLE_CLIENT_SECRET}
        scopes = "https://www.googleapis.com/auth/gmail.readonly,https://www.googleapis.com/auth/gmail.send,https://www.googleapis.com/auth/gmail.modify,https://www.googleapis.com/auth/userinfo.email,https://www.googleapis.com/auth/userinfo.profile"
    }
    whisper {
        apiKey = ${?OPENAI_API_KEY}
        apiUrl = "https://api.openai.com/v1/audio/transcriptions"
        model = "whisper-1"
    }
    gemini {
        apiKey = ${?GEMINI_API_KEY}
        model = "gemini-1.5-flash"
    }
    database {
        url = ${?DB_URL}
        driver = "org.h2.Driver"
    }
}
